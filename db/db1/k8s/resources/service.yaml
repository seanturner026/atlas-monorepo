apiVersion: v1
kind: Service
metadata:
  name: db1-postgres
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  type: ClusterIP
  selector:
    app: db1-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
